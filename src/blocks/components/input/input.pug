mixin input(element)
    - const required = element.required
    
    -if (element.required == true)
        input.input(class=element.class type=element.type placeholder=element.placeholder required)
    - else 
        input.input(class=element.class type=element.type placeholder=element.placeholder)